{
  "name": "@subjektify-dev/typescript",
  "version": "0.0.1",
  "description": "Subjektify's typescript plugin to be used with the subjektify cli",
  "main": "dist/index",
  "author": "subjektify-dev",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf dist",
    "compile": "tsc",
    "lint": "eslint .",
    "patch": "npm version patch && npm publish",
    "prepublishOnly": "npm run build",
    "test": "jest"
  },
  "keywords": [
    "dapp",
    "subjektify",
    "plugin"
  ],
  "devDependencies": {
    "@types/node": "^20.8.6",
    "eslint": "^8.51.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.1",
    "jest": "^29.7.0",
    "prettier": "^3.0.3",
    "rimraf": "^5.0.5",
    "subjektify": "^0.0.5",
    "typescript": "^5.2.2"
  }
}
